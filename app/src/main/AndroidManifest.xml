<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.bankmandiri.edc">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".MainApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:usesCleartextTraffic="true"
            android:theme="@style/AppTheme.NoActionBar">
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
                android:name=".view.LoginActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".view.RegisterInitiateActivity"
                android:label="Pendaftaran"
                android:theme="@style/AppTheme.NoActionBar"/>

        <activity
                android:name=".view.EdcInputActivity"
                android:label="Pendaftaran"
                android:theme="@style/AppTheme.NoActionBar"/>

        <activity
                android:name=".view.ShowQRActivity"
                android:label="QR Scan"/>

        <activity
                android:name=".view.CashWithdrawActivity"
                android:label="Tarik Tunai"/>

        <activity
                android:name=".view.TransferActivity"
                android:label="Kirim Uang"/>

        <activity
                android:name=".view.RegisterActivity"
                android:label="Pembukaan Rekening Online"/>

    </application>

</manifest>